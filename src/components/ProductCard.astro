---
import TechTag from './TechTag.astro';

interface Props {
  name: string;
  tagline: string;
  problem: string;
  features: string[];
  stack: string[];
}

const { name, tagline, problem, features, stack } = Astro.props;
---

<div class="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">
  <h2 class="text-2xl font-bold text-slate-900">{name}</h2>
  <p class="mt-2 text-base font-medium text-blue-600">{tagline}</p>

  <div class="mt-6">
    <h3 class="text-sm font-semibold uppercase tracking-wide text-slate-400">解決的問題</h3>
    <p class="mt-2 text-sm leading-relaxed text-slate-600">{problem}</p>
  </div>

  <div class="mt-6">
    <h3 class="text-sm font-semibold uppercase tracking-wide text-slate-400">核心功能</h3>
    <ul class="mt-2 space-y-1.5">
      {features.map((f) => (
        <li class="text-sm text-slate-600">
          <span class="mr-2 text-blue-400">&bull;</span>{f}
        </li>
      ))}
    </ul>
  </div>

  <div class="mt-6 flex flex-wrap gap-2">
    {stack.map((s) => <TechTag label={s} />)}
  </div>
</div>
